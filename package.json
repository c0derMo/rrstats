{
  "name": "RRPlayerStatistics",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@hapi/cookie": "^11.0.2",
    "@hapi/hapi": "^20.1.3",
    "@hapi/inert": "^6.0.3",
    "axios": "^0.24.0",
    "bcrypt": "^5.0.1",
    "csv-parse": "^5.0.4",
    "dotenv": "^10.0.0",
    "luxon": "^2.0.2",
    "mongoose": "^6.0.13",
    "twitter-api-v2": "^1.8.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "node out/index.js",
    "deploy": "yarn build && cp -R -f out deploy && cp run.sh deploy && cp yarn.lock deploy && cp package.json deploy && cp -R -f html deploy/html",
    "clean": "rm -r out deploy",
    "clean-win": "rmdir /S /Q out deploy",
    "deploy-win": "yarn build && xcopy out deploy /s /i /y && xcopy run.sh deploy /y && xcopy yarn.lock deploy /y && xcopy package.json deploy /y && xcopy html deploy\\html /s /i /y",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/luxon": "^2.0.7",
    "@types/mongoose": "^5.11.97",
    "@types/node": "^15.6.0",
    "@typescript-eslint/eslint-plugin": "^5.9.1",
    "@typescript-eslint/parser": "^5.9.1",
    "eslint": "^8.6.0",
    "eslint-plugin-vue": "^8.3.0",
    "typescript": "^4.5.4"
  }
}
